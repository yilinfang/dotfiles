#!/usr/bin/env bash
# Add a encrypted shared file to chezmoi, encrypted with a shared key.

set -euo pipefail

# Define the path to the shared key
SHARED_KEY_PATH="{{- .shared_pub_key_path -}}"

exec chezmoi add --encrypt --age-recipient-file "$SHARED_KEY_PATH" "$@"
