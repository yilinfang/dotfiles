-- lua/config/mini-statusline.lua
-- Configuration for `mini.statusline`
-- NOTE: `mini.icons` and `gitsigns.nvim` must be loaded before `mini.statusline`

local statusline = require('mini.statusline')
local opts = {}
statusline.setup(opts)
